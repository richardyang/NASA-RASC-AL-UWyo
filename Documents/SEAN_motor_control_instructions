I. ssh into motherboard, run roscore
  1) In "RoboOps-Senior-Design" directory:
    $ . export_ROS_PACKAGE_PATH
    $ export ROS_MASTER_URI="http://<IP_ADDRESS_OF_MOTHERBOARD>:11311"
    $ export ROS_IP=<IP_ADDRESS_OF_MOTHERBOARD>
    $ roscore &
    $ control-c

II. Compile the "manual_keyboard_control" (in Source/RoboOps_ControllerCode/)
  1) In "Source/RoboOps_ControllerCode/manual_keyboard_control/build"
    $ rm CMakeCache.txt
  2) In "Source/RoboOps_ControllerCode/manual_keyboard_control/"
    [to move up one directory: $ cd ..]
    $ make && rosmake

III. Run the manual control packages
  1) In the current terminal
    $ rosrun manual_keyboard_control manual_keyboard_control
  2) In a new terminal, ssh again, and in "RoboOps-Senior-Design" directory
    $ export ROS_MASTER_URI="http://<IP_ADDRESS_OF_MOTHERBOARD>:11311"
    $ export ROS_IP=<IP_ADDRESS_OF_MOTHERBOARD>
    $ . export_ROS_PACKAGE_PATH
    $ rosrun mamanual_keyboard_control arduino_command_translator

IV. Run another terminal, ssh into motherboard, and run python serial script:
  $ rosrun rosserial_python serial_node _port:=/dev/<PORT>      [usually ttyS0 or ttyS1]

IV. In a new terminal set ROS variables on local computer:
  $ export ROS_MASTER_URI="http://<IP_ADDRESS_OF_MOTHERBOARD>"
  $ export ROS_IP=<IP_ADDRESS_OF_MOTHERBOARD>

V. Run the keyboard ROS node on local computer:
  $ rosrun keyboard keyboard
